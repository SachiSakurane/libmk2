//
// Created by Himatya on 2018-12-06.
//

#pragma once

#include <type_traits>
#include <utility>

#include <mk2/iterator/size.hpp>

namespace mk2{
namespace math{
/*
    namespace detail
    {
        template<class Type>
        inline constexpr Type lagerp_impl(const Type x, const Type std::size_t size)
        {
            Type ret = static_cast<Type>(0);
            auto size = mk2::iterator::size(c);

            for (std::size_t i = 0; i < size; ++i)
            {
                Type a = c[i];

            }

            return ret;
        }
    }

    template<class Type, class Container, typename = typename std::enable_if<std::is_floating_point<Type>::value>::type>
    inline constexpr Type lagerp(const Type x, const Container& c, std::size_t size)
    {
        Type ret = static_cast<Type>(0);
        auto size = mk2::iterator::size(c);

        for (std::size_t i = 0; i < size; ++i)
        {
            Type a = c[i];

        }

        return ret;
    }
    */
}
}